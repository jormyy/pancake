# browser-smoke, tab-only mode (no E2E_BROWSER_FULL_SWEEP), branch dist pancake-553373c32e66-b8d17621a7a1
exit 1
Workflow roster-review-manage did not reach its ready state

# same scenario, rerun on branch dist: exit 1 (same error)
# same scenario on BASELINE main dist (2909a0a): exit 1
Workflow roster-review-manage did not reach its ready state

# E2E_BROWSER_FULL_SWEEP=1 npm run e2e:browser-smoke (branch dist): exit 0, status PASS (1:26 wall)
# cause: tests/e2e/browser-smoke.mjs only inserts the roster fixture row in fullSweep mode; roster.tsx renders the 'Set lineup automatically' control only when roster.length > 0, so tab-only smoke cannot reach the roster ready state on a freshly seeded league
