# Pancake Feedback Review Hardening Ledger

Source: `/Users/michaelchen/Downloads/Goals/pancake/pancake-feedback-review-hardening-goal.txt`

Branch: `main`

PR state: no pull request found for branch `main` (`gh pr view --json number,title,url` exited 1).

Baseline verification:
- `npm run typecheck`: pass.
- `npm test`: pass, 42 files / 447 tests.

Address-review contract: each feedback ID below is treated as a synthetic review comment. Current final state is open until item-specific code, tests, browser evidence, deslop, audit, and a local commit prove closure.

| ID | Bucket | Status | Baseline Evidence | Target Files / Areas | Verification Plan | Final Evidence | Audit Verdict |
| --- | --- | --- | --- | --- | --- | --- | --- |
| F01 | Performance/UI | Open | Skeleton/placeholder patterns still present in major screens (`LeagueLoadingShell`, `TradeListPlaceholder`, `RosterLoadingShell`). Existing baseline checks pass. | `app/(tabs)/league.tsx`, `app/(tabs)/trades.tsx`, `app/(tabs)/roster.tsx`, data hooks, perf budgets. | Measure route loading with browser perf/smoke, remove major skeleton screens while retaining cached content, run `npm run perf:budget`, browser mobile+desktop navigation. | Pending. | Pending. |
| F02 | Realtime architecture | Open | Auction/rookie room use Supabase realtime plus polling fallback; broader shared-state surfaces still need a realtime coverage map. | `lib/draft.ts`, room hooks, league/trade/waiver/roster data hooks, realtime publications/migrations. | Build coverage map, add subscriptions/fallbacks, prove two manager sessions propagate draft, auction, trade, waiver, roster, and history changes. | Pending. | Pending. |
| F03 | Auction UI/realtime | In progress | Live auction room previously rendered a closed-nomination `Recent activity` strip, not active nomination bid history. | `app/(modals)/draft-room.tsx`, `lib/draft.ts`, bid query/types. | Two-manager browser realtime proof still pending in integration gate. | `getDraftState` now loads open-nomination `bids` rows with manager/team/amount/time; live auction room renders `Bid history` with high-bid marker and no-bid state. `npx vitest run tests/draft-auction-static.test.ts tests/waiver-auction-logic-static.test.ts`: pass. `npm run typecheck`: pass. | Targeted static audit passes; realtime browser proof pending. |
| F04 | Auction UI/history | In progress | History tab existed and showed winner/price, but bounded height/detail made it feel secondary. | `app/(modals)/draft-room.tsx`. | Browser mobile+desktop history scan still pending in integration gate. | History height expanded and each completed nomination row now includes nomination order/time plus winner/no-bid context and price. Auction static test guards the larger history height and order/time metadata. | Targeted static audit passes; browser proof pending. |
| F05 | Auction rule system | In progress | `place_auction_bid_atomic` validated bid/budget but had no auto-award path or unbeatable-bid rule. | `supabase/sql/functions/auction-lifecycle.sql`, `supabase/migrations/20260708000002_auction_unbeatable_bid_auto_award.sql`, auction static tests. | Real backend/browser auto-award scenario still pending in integration gate. | Real auction bids now reserve `$1` for each remaining active roster slot and auto-close the nomination after a valid high bid when no other manager can afford the next legal bid with roster reserve/cap math. Mock auto-close is intentionally excluded because mock rooms do not persist roster wins. `npm run check:db-function-sources`: pass. `npx vitest run tests/waiver-auction-logic-static.test.ts`: pass. | Targeted static/DB parity audit passes; backend/browser scenario proof pending. |
| F06 | Trade block UI/API | In progress | Trade block previously started with `League Trade Block`, included the current user's listings in the public list, and colocated owner controls there. Backend owner-only add/remove RPCs existed. | `app/(tabs)/trades.tsx`, `supabase/functions/api/trades.ts`, trade static tests. | Browser block tab proof still pending in integration gate. | Split block tab into `Your Listings`, `List Your Players`, `List Your Picks`, and public `League Trade Block`; public listings now exclude `myMemberId`; added explicit empty states and owner-only API/RPC static coverage. `npx vitest run tests/trades-screen-static.test.ts ...`: pass. `npm run typecheck`: pass. | Targeted static audit passes; browser proof pending. |
| F07 | Trade window rule | In progress | `isTradingClosed`, propose-trade UI copy, and SQL trade functions previously restricted trading to active/playoffs before deadline. | `lib/league.ts`, `app/(modals)/propose-trade.tsx`, `supabase/sql/functions/trade-negotiation.sql`, `supabase/migrations/20260708000001_trade_window_feedback_rules.sql`, trade-window tests. | Browser/API post-deadline and champion-finalized scenarios still pending in integration gate. | Client now opens setup/drafting/offseason, locks only active/playoff post-deadline; SQL proposal/acceptance guards use deadline-through-finalized-champion rule; accepted-trade processing no longer requires active/playoffs, only non-archived. `npm run check:db-function-sources`: pass. `npx vitest run tests/trade-window-rules-static.test.ts ...`: pass. `npm run typecheck`: pass. | Targeted static/DB parity audit passes; browser/API scenario proof pending. |
| F08 | Auction timer controls | In progress | Timer chips were fixed at 30/60/120 seconds; no custom option. SQL validation already allowed 5-3600 seconds. | `components/league/DraftChips.tsx`, `components/league/DraftSetupPanels.tsx`, `components/league/MockRoomsPanel.tsx`, `hooks/use-league-screen-state.ts`, `supabase/functions/api/draft.ts`, auction/mock RPCs. | Browser real + mock auction create/start/reload proof still pending in the integration gate. | Implemented 15s/30s presets plus custom numeric timer, normalized state to 5-3600 seconds, wired real auction/rookie setup and mock room creation. `npm run typecheck`: pass. `npx vitest run tests/fantasy-draft-auction-goals-static.test.ts`: pass (27 tests). | Targeted static audit passes; browser proof pending. |
| F09 | Other teams UI | In progress | Other-team roster modal is read-only with back/trade actions, but it is thinner than own roster and lacks shared stats treatment. | `app/(modals)/team-roster.tsx`, `components/roster/RosterItems.tsx`, roster stats fetch helpers. | Browser mobile+desktop other-team roster proof still pending in integration gate. | Added shared `ReadOnlyRosterPlayerItem`, grouped other-team rosters into active/IR/taxi sections with slot-order sorting, shared FPts/MIN stat maps, status badges, player links, and no roster mutation controls. `npm run typecheck`: pass. `npx vitest run tests/team-trade-player-context-static.test.ts tests/trades-screen-static.test.ts tests/roster-lineup-static.test.ts tests/dynasty-transactions-static.test.ts`: pass. | Targeted static audit passes; browser proof pending. |
| F10 | FAAB end-to-end | In progress | FAAB schema/RPC/UI and many static/E2E checks exist; full required `$0` bid + processing + browser proof still needs current-run evidence. | `lib/waivers.ts`, claim modal, waiver SQL/API, E2E waiver/soak. | Run browser waiver plus processing gate in final integration. | `$0` FAAB is now explicitly guarded across modal default/parsing, API min validation, and SQL claim create/edit entry points; release gate now submits/processes a real `$0` FAAB claim, verifies roster add, `faab_bid_won` history metadata/body, and unchanged balances. Existing release gate also covers high-bid wins, equal-bid priority tiebreaking, losing-bid non-deduction, edit/cancel, commissioner balance adjustment, and transaction state reads. `npm run typecheck`: pass. `npm run check:db-function-sources`: pass. `npx vitest run tests/waiver-auction-logic-static.test.ts`: pass. | Targeted static/backend audit passes; browser/release-gate execution pending. |
| F11 | Player stats on trade/other-team UI | In progress | Own roster has FP/min/stats; trade and other-team rows mostly show name/team/position only. | `components/trades/TradeCard.tsx`, `app/(modals)/propose-trade.tsx`, `app/(modals)/team-roster.tsx`, shared player-context helpers. | Browser mobile+desktop trade composer/card/review proof still pending in integration gate. | Added `lib/roster-stats.ts` as the shared FPts/MIN source, reused it from own roster, other-team roster, trade composer, trade cards, and trade block player rows; trade queries now include eligible positions, injury status, years experience, and stat enrichment. `npm run typecheck`: pass. Targeted static suite above: pass. | Targeted static audit passes; browser proof pending. |
| F12 | Veto config | In progress | Fixed 24-hour veto and 50%/commissioner behavior existed; no configurable persisted settings were found. | League schema/settings, `accept_trade_atomic`, `veto_trade_atomic`, settings UI/API/tests. | Browser commissioner-settings save and accepted-trade veto-mode scenarios still pending in integration gate. | Added persisted league veto settings (`trade_veto_mode`, `trade_veto_window_hours`, `trade_veto_threshold_percent`) with defaults preserving prior behavior; commissioner settings UI can edit them; `accept_trade_atomic` opens configured windows or completes immediately when disabled; `veto_trade_atomic` enforces disabled/commissioner/member-threshold modes; trade-card veto affordance follows league mode and commissioner role. `npm run typecheck`: pass. `npm run check:db-function-sources`: pass. `npm run check:edge-functions`: pass. `npx vitest run tests/trade-veto-config-static.test.ts tests/trade-window-rules-static.test.ts tests/dynasty-transactions-static.test.ts tests/trades-screen-static.test.ts`: pass. | Targeted static/DB/API audit passes; browser and live backend scenario proof pending. |
| F13 | Multi-team trades | Open | Trade model is two-party (`proposer_member_id`, `recipient_member_id`, `trade_items.side`) and composer supports one recipient. | Trade schema/RPC/API/UI/E2E. | Design and migrate participant/asset model, keep two-team compatibility, prove three-team and all-team UI/data support plus failure path. | Pending. | Pending. |
