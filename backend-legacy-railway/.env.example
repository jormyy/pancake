# Supabase admin key - never expose publicly. Legacy service-role JWTs are not accepted.
# Use the dashboard-revealed sb_secret_ value; CLI/API metadata IDs are not usable keys.
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
PANCAKE_SUPABASE_SECRET_KEY=sb_secret_your_revealed_secret_key
SUPABASE_SECRET_KEY=sb_secret_your_revealed_secret_key
# Used by jose for JWT verification — find in Supabase dashboard > Settings > API > JWT Secret
SUPABASE_JWT_SECRET=your_supabase_jwt_secret

# Admin user IDs (comma-separated) allowed to call sync/admin routes
ADMIN_USER_IDS=your_user_id_here

# Server
PORT=3000

# E2E upstream seams. Production leaves these unset.
NBA_CDN_BASE_URL=https://cdn.nba.com/static/json
SLEEPER_BASE_URL=https://api.sleeper.app/v1
