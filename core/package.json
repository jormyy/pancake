{
    "name": "@pancake/core",
    "version": "1.0.0",
    "private": true,
    "main": "./cjs/index.js",
    "types": "./src/index.ts",
    "scripts": {
        "build": "tsc && tsc -p tsconfig.cjs.json",
        "build:cjs": "tsc -p tsconfig.cjs.json",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "devDependencies": {
        "typescript": "^5.7.3",
        "vitest": "^4.1.9"
    }
}
